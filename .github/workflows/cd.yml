name: MERN Ecommerce CD

# Run after CI workflow completes
on:
  workflow_run:
    workflows:
      - MERN Ecommerce CI
    types:
      - completed
env:
  IMAGE_TAG: ${{ github.event.workflow_run.head_sha }}

jobs:

  update-manifests:

    # Run only when CI succeeds
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    runs-on: ubuntu-latest

    # Allow GitHub Actions to push changes
    permissions:
      contents: write

    steps:

      # Checkout repository
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          ref: main

      # Update backend image tag
      - name: Update Backend Image
        run: |
          sed -i \
          "s|aliahmadshah/mern-ecommerce-backend:.*|aliahmadshah/mern-ecommerce-backend:${IMAGE_TAG}|g" \
          k8s/back-end-deployment.yml

      # Update frontend image tag
      - name: Update Frontend Image
        run: |
          sed -i \
          "s|aliahmadshah/mern-ecommerce-frontend:.*|aliahmadshah/mern-ecommerce-frontend:${IMAGE_TAG}|g" \
          k8s/front-end-deployment.yml

      # Check manifest changes
      - name: Check Changes
        run: |
          git diff -- k8s/

      # Configure Git
      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      # Commit manifest changes
      - name: Commit Changes
        run: |
          git add k8s/
          git commit -m "Update Kubernetes image tags to ${IMAGE_TAG}"

      # Push changes to GitHub
      - name: Push Changes
        run: |
          git push origin main
